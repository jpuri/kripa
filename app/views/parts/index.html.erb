<h1>Listing parts</h1>

<table class="borderedTable">
  <tr>
    <th>Part Number</th>
    <th>Partname</th>
    <th>Weight</th>
  </tr>

<% @parts.each do |part| %>
  <tr onclick="openPage('parts', <%= part.id %>)">
    <td><%= part.number %></td>
    <td><%= part.name %></td>
    <td><%= part.weight %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Part', new_part_path %>
